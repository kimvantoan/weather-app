
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="styles.css">
  <title>Weather App</title>
</head>
<body>
  <form action="/" method="post">
    <label for="city">city name:</label><br>
    <input type="text" id="city" name="cityname"><br>
    <input type="submit" value="Submit">
  </form>
  <div class="container">
    <h1>Weather App</h1>
    <div class="weather-card">
      <%if(weathers){ %>
        <div class="location"><%=weathers.name%></div>
        <img src="http://openweathermap.org/img/w/<%= weathers.weather[0].icon %>.png" alt="Weather Icon">
        <div class="temperature"><%=Math.floor(weathers.main.temp-273.5)%>°C</div>
        <div class="conditions"><%=weathers.weather[0].description%></div>
      <%}%>
    </div>
  </div>
</body>
</html>

